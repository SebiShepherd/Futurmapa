# Ignore runtime configuration (local overrides)
config.json

# Runtime branding assets (logos, Favicons) bleiben lokal – das SVG-Sprite wird versioniert
assets/*
!assets/*.svg

# Node / editor / OS temporary files (optional)
node_modules/
.DS_Store
Thumbs.db

# Local custom stylesheet (not committed)
styles.custom.css
